<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Explorar abogados | Tu Estudio Juridico</title>
    <link rel="canonical" href="https://example.com/explorar">
    <meta name="description" content="Explora abogados por materia, región y comuna en Tu Estudio Juridico. Compara perfiles y contacta directamente al profesional indicado para tu caso.">
    <meta property="og:title" content="Explorar abogados | Tu Estudio Juridico">
    <meta property="og:description" content="Explora abogados por materia, región y comuna en Tu Estudio Juridico. Compara perfiles y contacta directamente al profesional indicado para tu caso.">
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://example.com/explorar">
    <meta property="og:image" content="https://example.com/og-lawyers-1200x630.png">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Explorar abogados | Tu Estudio Juridico">
    <meta name="twitter:description" content="Explora abogados por materia, región y comuna en Tu Estudio Juridico. Compara perfiles y contacta directamente al profesional indicado para tu caso.">
    <meta name="twitter:image" content="https://example.com/og-lawyers-1200x630.png">
    <link rel="icon" type="image/svg+xml" href="/favicon.svg">
    <link rel="stylesheet" href="/feed-page.css?v=2026030222">
    <link rel="stylesheet" href="/app-shell.css?v=2026022705">
    <script src="/app-shell.js?v=202602231" defer></script>
    <style>
        .market-desktop-only { display: none; }
        @media (min-width: 901px) {
            body.theme-black.theme-olive { background: #f5f7fb; }
            .wrap { max-width: 1320px; padding: 0 16px 136px; }
            .market-desktop-only { display: block; }
            .feed-topbar {
                margin-top: 8px;
                border-radius: 12px;
                background: #ffffff;
                border: 1px solid rgba(15, 23, 42, .12);
                box-shadow: 0 1px 2px rgba(15, 23, 42, .06);
            }
            .feed-topbar .title { font-size: 18px; color: #0f172a !important; }
            .feed-topbar .sub { color: #475569 !important; font-size: 12px; }

            .market-login-bar {
                margin-top: 10px;
                display: flex;
                justify-content: space-between;
                align-items: center;
                gap: 12px;
                flex-wrap: wrap;
                padding: 10px 12px;
                border: 1px solid rgba(15, 23, 42, .12);
                border-radius: 12px;
                background:
                    linear-gradient(90deg, rgba(10, 102, 194, .08), rgba(255, 255, 255, .96) 45%),
                    #ffffff;
            }
            .market-login-copy {
                font-size: 13px;
                color: #334155;
                font-weight: 700;
                line-height: 1.35;
            }
            .market-login-actions {
                display: flex;
                align-items: center;
                gap: 8px;
                flex-wrap: wrap;
            }
            .market-login-btn {
                font-size: 13px;
                font-weight: 700;
                border-radius: 999px;
                padding: 8px 14px;
                border: 1px solid #0a66c2;
                color: #ffffff;
                background: #0a66c2;
            }
            .market-login-btn.secondary {
                color: #0a66c2;
                background: #edf5ff;
                border-color: rgba(10, 102, 194, .28);
            }

            .market-desktop-hero {
                margin-top: 10px;
                border-radius: 14px;
                border: 1px solid rgba(15, 23, 42, .12);
                background:
                    radial-gradient(220px 140px at 4% 10%, rgba(10, 102, 194, .11), transparent 76%),
                    radial-gradient(220px 140px at 98% 20%, rgba(58, 138, 221, .1), transparent 78%),
                    #ffffff;
                padding: 18px 16px 16px;
                display: grid;
                gap: 14px;
            }
            .market-desktop-hero h1 {
                margin: 0;
                font-size: 28px;
                line-height: 1.1;
                color: #0f172a;
                letter-spacing: -.02em;
            }
            .market-desktop-hero p {
                margin: 5px 0 0;
                color: #475569;
                font-size: 14px;
                line-height: 1.36;
            }
            .market-search-form {
                display: grid;
                gap: 10px;
                grid-template-columns: 1.45fr 1fr 1fr auto;
                align-items: end;
            }
            .market-search-form label {
                display: grid;
                gap: 5px;
                font-size: 12px;
                color: #334155;
                font-weight: 700;
            }
            .market-search-form .input,
            .market-search-form select {
                min-height: 44px;
                border-radius: 11px;
                border: 1px solid rgba(15, 23, 42, .18);
                background: #ffffff;
                color: #0f172a;
                font-size: 14px;
            }
            .market-search-form .btn.btn-primary {
                min-height: 44px;
                border-radius: 11px;
                background: #0a66c2;
                border-color: #0a66c2;
                color: #ffffff;
                font-weight: 800;
                padding-inline: 18px;
            }
            .market-desktop-categories {
                margin-top: 12px;
                display: grid;
                gap: 10px;
                grid-template-columns: repeat(4, minmax(0, 1fr));
            }
            .market-category-card {
                border: 1px solid rgba(15, 23, 42, .12);
                border-radius: 12px;
                background: #ffffff;
                padding: 12px;
                display: grid;
                gap: 4px;
                min-height: 102px;
                box-shadow: 0 1px 2px rgba(15, 23, 42, .05);
            }
            .market-category-icon {
                display: inline-flex;
                align-items: center;
                justify-content: center;
                width: 28px;
                height: 28px;
                border-radius: 999px;
                background: #eaf3ff;
                color: #0a66c2;
                font-size: 15px;
            }
            .market-category-card strong {
                font-size: 15px;
                line-height: 1.15;
                color: #0f172a;
            }
            .market-category-card span {
                font-size: 12px;
                color: #475569;
                line-height: 1.3;
            }

            .feed-overview-strip { display: none !important; }
            .feed-quick-strip {
                margin-top: 10px !important;
                border-radius: 12px;
                border-color: rgba(15, 23, 42, .12) !important;
                background: #ffffff !important;
            }

            .market-desktop-filters {
                margin-top: 10px !important;
                border-radius: 12px !important;
                border-color: rgba(15, 23, 42, .12) !important;
                background: #ffffff !important;
                box-shadow: 0 1px 2px rgba(15, 23, 42, .06) !important;
            }
            .market-desktop-filters form.grid {
                grid-template-columns: 1.2fr 1fr 1fr 1fr auto !important;
                gap: 10px !important;
            }
            .market-desktop-filters label {
                font-size: 12px !important;
                color: #334155 !important;
            }
            .market-desktop-filters .input,
            .market-desktop-filters select {
                min-height: 42px;
                border-radius: 10px;
                border: 1px solid rgba(15, 23, 42, .18);
                background: #ffffff;
                color: #0f172a;
            }

            .result-head { margin: 14px 0 10px !important; }
            .result-head .section-title {
                font-size: 24px;
                letter-spacing: -.02em;
                color: #0f172a;
            }
            .result-head .hint { color: #475569; font-size: 13px; font-weight: 700; }
            .grid.desktop-grid.client-grid { grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 14px; }

            .feed-card-item {
                border-radius: 12px;
                overflow: hidden;
                background:
                    linear-gradient(180deg, rgba(10, 102, 194, .06), transparent 42%),
                    #ffffff !important;
                border: 1px solid rgba(15, 23, 42, .12) !important;
                box-shadow: 0 1px 2px rgba(15, 23, 42, .08), 0 8px 20px rgba(15, 23, 42, .06) !important;
            }
            .feed-card-item:hover {
                transform: translateY(-2px);
                border-color: rgba(10, 102, 194, .35) !important;
                box-shadow: 0 3px 8px rgba(15, 23, 42, .11), 0 14px 24px rgba(10, 102, 194, .12) !important;
            }
            .feed-card-head {
                display: block;
                padding: 0;
            }
            .media-frame {
                width: 100%;
                height: 206px;
                border-radius: 0;
                border: 0;
                box-shadow: none;
                margin-inline: 0;
            }
            .media-frame.no-photo { border-bottom: 1px solid rgba(15, 23, 42, .08); }
            .feed-card-head-content { padding: 10px 12px 0; }
            .card-topline { display: none; }
            .card-title {
                text-align: left;
                font-size: 19px;
                min-height: 0;
                color: #0f172a !important;
                line-height: 1.16;
            }
            .card-sub {
                text-align: left;
                font-size: 13px;
                line-height: 1.3;
                color: #475569 !important;
            }
            .card-specialty {
                margin-top: 4px;
                text-align: left;
                font-size: 19px !important;
                line-height: 1.18;
                color: #0a66c2 !important;
                -webkit-text-fill-color: #0a66c2 !important;
                text-shadow: none !important;
            }
            .card-body {
                padding: 10px 12px 14px;
                gap: 8px;
            }
            .card-mode-line { order: 1; }
            .card-meta-min {
                order: 2;
                text-align: left;
                color: #475569 !important;
                font-size: 13px;
                line-height: 1.35;
            }
            .card-stats { display: none; }
            .card-extra-line .mini-pill {
                color: #0a66c2 !important;
                background: #edf5ff !important;
                border-color: rgba(10, 102, 194, .22) !important;
            }
            .card-mode-pill.region-strong {
                background: #0a66c2 !important;
                color: #ffffff !important;
                border-color: #0a66c2 !important;
            }
            .bottom-nav,
            .google-login-banner {
                display: none !important;
            }
        }
        @media (max-width: 900px) {
            .market-desktop-only { display: none !important; }
        }
        .explorar-footnote {
            margin-top: 18px;
            text-align: center;
            font-size: 12px;
            color: #64748b;
            line-height: 1.35;
        }
        .explorar-footnote a {
            color: #0a66c2;
            text-decoration: none;
            font-weight: 700;
        }
        .explorar-footnote a:hover { text-decoration: underline; }
        @media (max-width: 900px) {
            .explorar-footnote {
                margin-top: 14px;
                margin-bottom: 6px;
                font-size: 11px;
            }
        }
    </style>
</head>
<?php
    $themeEmail = strtolower(trim((string)($_SESSION['email'] ?? '')));
    $isAdminTheme = ($themeEmail === 'gmcalderonlewin@gmail.com');
    $isClientTheme = !$isAdminTheme && !empty($_SESSION['user_id']) && (($_SESSION['rol'] ?? 'cliente') === 'cliente');
    $feedBodyClass = 'theme-black' . ($isAdminTheme ? ' theme-admin' : ' theme-olive');
?>
<?php
    $feedModeView = (string)($feed_mode ?? (!empty($_SESSION['user_id']) && (($_SESSION['rol'] ?? 'cliente') === 'abogado') ? 'abogado' : 'cliente'));
    $isLawyerFeed = $feedModeView === 'abogado';
    $isClientFeed = $feedModeView === 'cliente';
    $currentFeedUser = is_array($current_user ?? null) ? $current_user : [];
    $currentFeedSlug = trim((string)($currentFeedUser['slug'] ?? ''));
    $displayAccountName = trim((string)($_SESSION['nombre'] ?? $_SESSION['user_name'] ?? 'Mi cuenta'));
    $lawyerFeedLeadsState = is_array($lawyer_feed_leads ?? null) ? $lawyer_feed_leads : ['pending' => 0, 'total' => 0, 'pending_new' => 0, 'pending_seen' => 0];
    $lawyerPendingLeads = (int)($lawyerFeedLeadsState['pending'] ?? 0);
    $lawyerTotalLeads = (int)($lawyerFeedLeadsState['total'] ?? 0);
    $lawyerPendingNewLeads = (int)($lawyerFeedLeadsState['pending_new'] ?? $lawyerPendingLeads);
    $lawyerPendingSeenLeads = (int)($lawyerFeedLeadsState['pending_seen'] ?? max(0, $lawyerPendingLeads - $lawyerPendingNewLeads));
    $lawyerProfileActive = (int)($currentFeedUser['activo'] ?? 0) === 1;
?>
<body class="<?= htmlspecialchars($feedBodyClass) ?>">
    <main class="wrap">
        <?php if ($isLawyerFeed): ?>
            <section class="lawyer-update-banner" aria-label="Opciones de perfil profesional">
                <div class="txt"><strong><?= $lawyerProfileActive ? 'Perfil profesional activo.' : 'Perfil profesional oculto.' ?></strong> <?= $lawyerProfileActive ? 'Gestiona tu publicación y mantén tus datos al día.' : 'Tu perfil no aparece en el listado público. Puedes reactivarlo de inmediato.' ?>
                    <span class="profile-visibility-badge <?= $lawyerProfileActive ? 'is-published' : 'is-hidden' ?>"><?= $lawyerProfileActive ? 'Publicado' : 'Oculto' ?></span>
                    <?php if ($lawyerPendingNewLeads > 0): ?>
                        <span class="app-badge badge-warn" style="margin-left:6px;">Tienes <?= (int)$lawyerPendingNewLeads ?> lead<?= $lawyerPendingNewLeads === 1 ? '' : 's' ?> nuevo<?= $lawyerPendingNewLeads === 1 ? '' : 's' ?></span>
                    <?php elseif ($lawyerPendingSeenLeads > 0): ?>
                        <span class="app-badge badge-info" style="margin-left:6px;">Tienes <?= (int)$lawyerPendingSeenLeads ?> lead<?= $lawyerPendingSeenLeads === 1 ? '' : 's' ?> sin contactar</span>
                    <?php endif; ?>
                </div>
                <div class="banner-actions">
                    <a class="btn-mini btn-mini-primary" href="/dashboard/home">Ir al dashboard</a>
                    <?php if ($lawyerPendingLeads > 0): ?>
                        <a class="btn-mini" href="/dashboard/leads" style="border-color:<?= $lawyerPendingNewLeads > 0 ? 'rgba(255,214,102,.45)' : 'rgba(138,169,255,.35)' ?>; color:<?= $lawyerPendingNewLeads > 0 ? '#ffe09a' : '#d7e5ff' ?>;">
                            <?= $lawyerPendingNewLeads > 0 ? 'Ver leads nuevos' : 'Ver leads sin contactar' ?> (<?= (int)$lawyerPendingLeads ?>)
                        </a>
                    <?php endif; ?>
                    <a class="btn-mini" href="/completar-datos?modo=abogado">Editar perfil</a>
                    <a class="btn-mini" href="<?= htmlspecialchars($currentFeedSlug !== '' ? ('/' . $currentFeedSlug) : '/mi-tarjeta') ?>">Ver mi perfil</a>
                    <?php if ($lawyerProfileActive): ?>
                        <form method="POST" action="/desactivar-perfil-profesional" onsubmit="return confirm('¿Desactivar temporalmente tu perfil profesional?');" style="margin:0;">
                            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars((string)($csrf_token ?? '')) ?>">
                            <button type="submit" class="btn-mini btn-mini-danger" style="cursor:pointer;">Desactivar perfil</button>
                        </form>
                    <?php else: ?>
                        <form method="POST" action="/reactivar-perfil-profesional" style="margin:0;">
                            <input type="hidden" name="csrf_token" value="<?= htmlspecialchars((string)($csrf_token ?? '')) ?>">
                            <button type="submit" class="btn-mini btn-mini-success" style="cursor:pointer;">Reactivar perfil</button>
                        </form>
                    <?php endif; ?>
                </div>
            </section>
        <?php endif; ?>
        <section class="feed-topbar app-bar">
            <div class="feed-topbar-left">
                <?php if (!$isLawyerFeed): ?>
                <div style="display:flex; gap:6px; flex-wrap:wrap;"></div>
                <?php endif; ?>
                <div class="title"><?= $isLawyerFeed ? 'Tu Estudio Juridico Generando oportunidades!' : 'Tu Estudio Juridico · Busca tu abogado' ?></div>
                <?php if ($isLawyerFeed): ?>
                <div class="sub">Revisa el directorio y mantén tu perfil profesional actualizado.</div>
                <?php else: ?>
                <div class="sub">Filtra por materia, región o ciudad y abre perfiles para comparar.</div>
                <?php endif; ?>
            </div>
            <?php if (!empty($_SESSION['user_id'])): ?>
                <div style="display:flex;gap:8px;align-items:center;"> 
                    <div class="account-pill" title="Sesión activa">
                        <span class="dot" aria-hidden="true"></span>
                        <span><?= htmlspecialchars($displayAccountName) ?></span>
                    </div>
                    <a class="btn btn-ghost" href="/logout">Salir</a>
                </div>
            <?php endif; ?>
        </section>
        <?php if ($isClientFeed): ?>
        <section class="market-login-bar market-desktop-only" aria-label="Barra de acceso">
            <?php if (empty($_SESSION['user_id'])): ?>
                <div class="market-login-copy">Explora libremente y abre perfiles sin login. Inicia sesión con Google para enviar tu formulario al abogado.</div>
                <div class="market-login-actions">
                    <a href="/login-google?next=%2Fexplorar" class="market-login-btn">Iniciar sesión con Google</a>
                </div>
            <?php else: ?>
                <div class="market-login-copy">Sesión activa como <?= htmlspecialchars($displayAccountName) ?>. Puedes contactar abogados desde los perfiles.</div>
                <div class="market-login-actions">
                    <a href="/panel" class="market-login-btn secondary">Ir a mi panel</a>
                    <a href="/logout" class="market-login-btn secondary">Cerrar sesión</a>
                </div>
            <?php endif; ?>
        </section>

        <section class="market-desktop-hero market-desktop-only" aria-label="Búsqueda principal de abogados">
            <div>
                <h1>¿Qué estás buscando?</h1>
                <p>Encuentra abogados por especialidad y región en Chile. Explora perfiles libremente y registra tu cuenta para enviar mensajes.</p>
            </div>
            <form method="GET" action="/explorar" class="market-search-form">
                <label>
                    ¿Qué buscas?
                    <select class="input" name="especialidad">
                        <option value="">Todas las especialidades</option>
                        <?php foreach (($materias_catalogo ?? ['Familia', 'Penal', 'Laboral', 'Civil']) as $mat): ?>
                            <option value="<?= htmlspecialchars($mat) ?>" <?= (($filtroActual ?? '') === $mat) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($mat) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <label>
                    Ubicación
                    <select class="input" name="region">
                        <option value="">Todas las regiones</option>
                        <option value="Todo Chile" <?= (($region ?? '') === 'Todo Chile') ? 'selected' : '' ?>>Todo Chile</option>
                        <?php foreach (($regiones_chile ?? []) as $regionItem): ?>
                            <option value="<?= htmlspecialchars($regionItem) ?>" <?= (($region ?? '') === $regionItem) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($regionItem) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <label>
                    Comuna
                    <input class="input" type="text" name="comuna" value="<?= htmlspecialchars((string)($comuna ?? '')) ?>" placeholder="Ej: Providencia">
                </label>
                <button class="btn btn-primary" type="submit">Buscar</button>
            </form>
        </section>

        <section class="market-desktop-categories market-desktop-only" aria-label="Accesos por categoría legal">
            <a class="market-category-card" href="/explorar?especialidad=Derecho%20Familiar">
                <span class="market-category-icon" aria-hidden="true">👨‍👩‍👧</span>
                <strong>Familia</strong>
                <span>Divorcio, alimentos, cuidado personal y relación directa.</span>
            </a>
            <a class="market-category-card" href="/explorar?especialidad=Derecho%20Penal">
                <span class="market-category-icon" aria-hidden="true">🏛️</span>
                <strong>Penal</strong>
                <span>Defensa penal, querellas y representación ante tribunales.</span>
            </a>
            <a class="market-category-card" href="/explorar?especialidad=Derecho%20Laboral">
                <span class="market-category-icon" aria-hidden="true">💼</span>
                <strong>Laboral</strong>
                <span>Despidos, tutelas laborales y negociación de finiquitos.</span>
            </a>
            <a class="market-category-card" href="/explorar?especialidad=Derecho%20Civil">
                <span class="market-category-icon" aria-hidden="true">📄</span>
                <strong>Civil</strong>
                <span>Contratos, responsabilidad civil y juicios patrimoniales.</span>
            </a>
        </section>
        <?php endif; ?>
        <?php $pageNum = (int)($page ?? 1); ?>
        <?php if (!$isLawyerFeed): ?>
        <section class="feed-info-strip feed-overview-strip<?= $pageNum > 1 ? ' compact' : '' ?>"<?= $pageNum >= 3 ? ' style="display:none;"' : '' ?>>
            <div class="row-top">
                <div>
                    <div class="label">Cómo funciona</div>
                    <div class="desc">Directorio legal para buscar y contactar abogados</div>
                </div>
            </div>
            <div class="stats">
                <div class="stat">
                    <small>Profesionales registrados</small>
                    <strong>+<?= number_format((int)($stats_explorar['profesionales'] ?? 0), 0, ',', '.') ?></strong>
                    <div class="desc" style="margin-top:3px;"><?= number_format((int)($stats_explorar['profesionales'] ?? 0), 0, ',', '.') ?> abogados de <?= number_format((int)($stats_explorar['materias'] ?? 0), 0, ',', '.') ?> materias</div>
                </div>
                <div class="stat">
                    <small>Clientes que buscan o han buscado abogado</small>
                    <strong><?= number_format((int)($stats_explorar['clientes'] ?? 0), 0, ',', '.') ?></strong>
                </div>
            </div>
        </section>
        <?php endif; ?>

        <?php if ($isClientFeed): ?>
        <?php $hasActiveSearchFilters = ((string)($filtroActual ?? '') !== '' || (string)($region ?? '') !== '' || (string)($comuna ?? '') !== '' || (string)($sexo ?? '') !== ''); ?>
        <section class="feed-info-strip feed-quick-strip" style="margin-top:6px;">
            <div class="row-top">
                <div>
                    <div class="label">Búsquedas rápidas</div>
                    <div class="desc">Toca un chip para filtrar y comparar perfiles en segundos.</div>
                </div>
                <?php if ($hasActiveSearchFilters): ?>
                    <a class="mini-pill app-badge badge-info" href="/explorar" style="text-decoration:none; white-space:nowrap;">Reiniciar búsquedas</a>
                <?php endif; ?>
            </div>
            <div class="card-extra-line">
                <?php $quickSearches = array_values(array_filter((array)($quick_searches ?? []))); ?>
                <?php if (!empty($quickSearches)): ?>
                    <?php foreach ($quickSearches as $qs): ?>
                        <a class="mini-pill app-badge badge-info" href="<?= htmlspecialchars((string)($qs['href'] ?? '/explorar')) ?>"><?= htmlspecialchars((string)($qs['label'] ?? 'Ver abogados')) ?></a>
                    <?php endforeach; ?>
                <?php else: ?>
                    <a class="mini-pill app-badge badge-info" href="/explorar?especialidad=Derecho%20Penal&region=Regi%C3%B3n%20de%20Coquimbo">Penalista en La Serena</a>
                    <a class="mini-pill app-badge badge-info" href="/explorar?especialidad=Derecho%20Familiar&region=Regi%C3%B3n%20Metropolitana%20de%20Santiago">Abogada/o de familia en Santiago</a>
                    <a class="mini-pill app-badge badge-info" href="/explorar?especialidad=Derecho%20Laboral&region=Regi%C3%B3n%20de%20Valpara%C3%ADso">Laboral en Valparaíso</a>
                    <a class="mini-pill app-badge badge-info" href="/explorar?especialidad=Derecho%20Civil&region=Todo%20Chile">Civil en todo Chile</a>
                <?php endif; ?>
            </div>
        </section>
        <?php endif; ?>

        <div class="filter-backdrop mobile-only" id="filterBackdrop"></div>

        <section class="filter-form feed-mobile-filters market-desktop-filters" style="margin-top: 6px;">
            <div class="filter-drag-handle" id="filterDragHandle" aria-hidden="true"></div>
            <div class="filter-sheet-head">
                <strong>Filtros</strong>
                <button type="button" class="filter-sheet-close" id="closeFiltersBtn">Cerrar</button>
            </div>
            <form method="GET" action="/explorar" class="grid" style="grid-template-columns: repeat(5, minmax(0, 1fr)); align-items:end;">
                <label>
                    Materia
                    <select class="input" name="especialidad">
                        <option value="">Todos los abogados</option>
                        <?php foreach (($materias_catalogo ?? ['Familia', 'Penal', 'Laboral', 'Civil']) as $mat): ?>
                            <option value="<?= htmlspecialchars($mat) ?>" <?= (($filtroActual ?? '') === $mat) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($mat) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <label>
                    Región
                    <select class="input" name="region">
                        <option value="">Todas las regiones</option>
                        <option value="Todo Chile" <?= (($region ?? '') === 'Todo Chile') ? 'selected' : '' ?>>Todo Chile</option>
                        <?php foreach (($regiones_chile ?? []) as $regionItem): ?>
                            <option value="<?= htmlspecialchars($regionItem) ?>" <?= (($region ?? '') === $regionItem) ? 'selected' : '' ?>>
                                <?= htmlspecialchars($regionItem) ?>
                            </option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <label>
                    Ciudad
                    <input class="input" type="text" name="comuna" value="<?= htmlspecialchars((string)($comuna ?? '')) ?>" placeholder="Ej: La Serena">
                </label>
                <label>
                    Sexo
                    <select class="input" name="sexo">
                        <option value="">Todos</option>
                        <?php $sexoLabels = ['mujer'=>'Mujer','hombre'=>'Hombre','no_binario'=>'No binario','prefiero_no_decir'=>'Prefiere no decir']; ?>
                        <?php foreach (($sexos_catalogo ?? []) as $sx): ?>
                            <option value="<?= htmlspecialchars($sx) ?>" <?= (($sexo ?? '') === $sx) ? 'selected' : '' ?>><?= htmlspecialchars($sexoLabels[$sx] ?? $sx) ?></option>
                        <?php endforeach; ?>
                    </select>
                </label>
                <div style="display:flex; gap:8px; align-items:center; justify-content:flex-end;">
                    <a class="btn" href="/explorar" style="white-space:nowrap;">Limpiar filtros y búsquedas</a>
                    <button class="btn btn-primary" type="submit" style="white-space:nowrap;">Aplicar filtros</button>
                </div>
            </form>
        </section>

        <?php if (!empty($no_results_fallback)): ?>
            <div class="feed-fallback-note">
                <strong>Sin resultados exactos.</strong> <?= htmlspecialchars((string)($no_results_message ?? 'Te mostramos abogados disponibles para que explores alternativas.')) ?>
            </div>
        <?php endif; ?>

        <?php if(empty($abogados)): ?>
            <div class="empty" style="margin-top: 16px;">
                <h3>No encontramos matchs con ese filtro</h3>
                <p class="muted">Prueba otra materia o región.</p>
                <a class="btn btn-primary" href="/explorar">Ver todos los perfiles</a>
            </div>
        <?php else: ?>
            <?php
                $isTopUniversity = static function ($universidad): bool {
                    $raw = trim((string)$universidad);
                    $u = function_exists('mb_strtolower') ? mb_strtolower($raw, 'UTF-8') : strtolower($raw);
                    if ($u === '') return false;
                    return str_contains($u, 'universidad de chile')
                        || str_contains($u, 'pontificia universidad catolica de chile')
                        || str_contains($u, 'pontificia universidad católica de chile')
                        || str_contains($u, 'puc chile')
                        || str_contains($u, 'uc chile');
                };
                $buildFeedCardSummary = static function(array $abg, string $specLabel, array $subm): string {
                    $nombre = trim((string)($abg['nombre'] ?? ''));
                    $uni = trim((string)($abg['universidad'] ?? 'Universidad no informada'));
                    $uniPhrase = preg_match('/^universidad\b/i', $uni) ? $uni : ('Universidad ' . $uni);
                    $exp = trim((string)($abg['experiencia'] ?? ''));
                    $materia = trim((string)($abg['especialidad'] ?? $specLabel));
                    $sub = trim((string)($subm[0] ?? ''));
                    $expTxt = $exp !== '' ? $exp : 'experiencia informada';
                    $txt = 'Abogado ' . ($nombre !== '' ? $nombre : 'disponible') . ', licenciado de ' . $uniPhrase . ', con más de ' . $expTxt . ' de experiencia se especializa en ' . $materia;
                    if ($sub !== '') { $txt .= ', específicamente en ' . $sub; }
                    $txt .= '.';
                    $txt = preg_replace('/\s+/u', ' ', trim($txt));
                    if (function_exists('mb_strlen') && mb_strlen($txt, 'UTF-8') > 132) {
                        $txt = rtrim(mb_substr($txt, 0, 129, 'UTF-8')) . '...';
                    } elseif (strlen($txt) > 132) {
                        $txt = rtrim(substr($txt, 0, 129)) . '...';
                    }
                    if (!str_ends_with($txt, '...')) $txt .= '...';
                    return $txt;
                };
                $bioVisible = static function(array $abg): string {
                    $bio = trim((string)($abg['bio'] ?? 'Atención directa y enfoque práctico para ayudarte a resolver tu caso.'));
                    if ($bio === '') { $bio = 'Atención directa y enfoque práctico para ayudarte a resolver tu caso.'; }
                    $words = preg_split('/\s+/u', $bio, -1, PREG_SPLIT_NO_EMPTY) ?: [];
                    $short = implode(' ', array_slice($words, 0, 10));
                    return rtrim($short) . (count($words) > 10 ? '...' : '');
                };
                $plazaResumen = static function ($ciudadesPlaza): string {
                    $raw = trim((string)$ciudadesPlaza);
                    if ($raw === '') return '';
                    $parts = array_values(array_filter(array_map('trim', explode(',', $raw))));
                    if (empty($parts)) return '';
                    if (count($parts) <= 2) return implode(', ', $parts);
                    return implode(', ', array_slice($parts, 0, 2)) . ' +' . (count($parts) - 2);
                };
                $destacadosPrioritarios = array_values(array_filter($abogados, function ($x) {
                    return !empty($x['destacado_hasta']) && strtotime((string)$x['destacado_hasta']) >= time();
                }));
                $destacados = !empty($destacadosPrioritarios)
                    ? array_slice($destacadosPrioritarios, 0, (count($destacadosPrioritarios) >= 6 ? 6 : count($destacadosPrioritarios)))
                    : (!empty($top_por_especialidad) ? $top_por_especialidad : array_slice($abogados, 0, 3));
                $brandRingMap = [
                    'azul' => 'rgba(92,142,214,.75)',
                    'verde' => 'rgba(63,208,168,.78)',
                    'dorado' => 'rgba(216,194,165,.82)',
                    'rosa' => 'rgba(239,109,168,.82)',
                    'vino' => 'rgba(180,91,122,.82)',
                    'grafito' => 'rgba(127,142,163,.75)',
                ];
                $brandTintMap = [
                    'azul' => 'rgba(92,142,214,.36)',
                    'verde' => 'rgba(63,208,168,.36)',
                    'dorado' => 'rgba(216,194,165,.38)',
                    'rosa' => 'rgba(239,109,168,.36)',
                    'vino' => 'rgba(180,91,122,.36)',
                    'grafito' => 'rgba(127,142,163,.35)',
                ];
                $destacadosIds = array_map('intval', array_column($destacados, 'id'));
                $abogadosRestantes = array_values(array_filter($abogados, function ($abg) use ($destacadosIds) {
                    return !in_array((int)$abg['id'], $destacadosIds, true);
                }));
            ?>
            <section style="margin-top: 6px;">
                <div class="result-head" style="margin: 0 0 8px;">
                    <h2 class="section-title" style="margin:0;">Quizás te podría interesar...</h2>
                    <button type="button" class="filter-ticket mobile-only" id="toggleMobileFiltersBtn"><span class="dot" aria-hidden="true"></span>Mostrar filtros</button>
                </div>
                <div class="grid desktop-grid<?= $isClientFeed ? " client-grid" : "" ?>">
                    <?php foreach ($destacados as $abg): ?>
                    <?php $brandKey = trim((string)($abg['color_marca'] ?? 'azul')); ?>
                    <?php $brandRing = $brandRingMap[$brandKey] ?? $brandRingMap['azul']; ?>
                    <?php $brandTint = $brandTintMap[$brandKey] ?? $brandTintMap['azul']; ?>
                    <a href="/<?= $abg['slug'] ?>" class="card card-hover feed-card-item is-featured" style="display:block; --brand-ring: <?= htmlspecialchars($brandRing) ?>; --brand-tint: <?= htmlspecialchars($brandTint) ?>;">
                            <?php $fotoSrc = trim((string)($abg['foto_publica'] ?? '')); ?>
                            <?php
                                $abogadoNombreAlt = trim((string)($abg['nombre'] ?? ''));
                                $abogadoEspecialidadAlt = trim((string)($abg['especialidad'] ?? ''));
                                $fotoAlt = $abogadoNombreAlt !== ''
                                    ? ('Foto de perfil de ' . $abogadoNombreAlt . ($abogadoEspecialidadAlt !== '' ? ', abogado/a en ' . $abogadoEspecialidadAlt : ''))
                                    : 'Foto de perfil de abogado';
                            ?>
                            <div class="feed-card-head">
                            <?php if ($fotoSrc !== ''): ?>
                                <div class="media-frame" style="--brand-ring: <?= htmlspecialchars($brandRing) ?>">
                                    <img src="<?= htmlspecialchars($fotoSrc) ?>" alt="<?= htmlspecialchars($fotoAlt) ?>" style="width:100%; aspect-ratio:1/1; object-fit:cover;" loading="lazy" onerror="this.closest('.media-frame').classList.add('no-photo','demo-photo'); this.remove();">
                                </div>
                            <?php else: ?>
                                <div class="media-frame no-photo demo-photo" style="--brand-ring: <?= htmlspecialchars($brandRing) ?>">
                                    <div class="avatar-fallback"><?= htmlspecialchars(mb_strtoupper(mb_substr((string)($abg['nombre'] ?? 'A'), 0, 1))) ?></div>
                                </div>
                            <?php endif; ?>
                        <div class="feed-card-head-content">
                            <?php
                                $spec = (string)($abg['especialidad'] ?? '');
                                $specMap = [
                                    'Derecho Penal' => ['⚖️', 'Derecho Penal'],
                                    'Penal' => ['⚖️', 'Derecho Penal'],
                                    'Derecho Laboral' => ['💼', 'Derecho Laboral'],
                                    'Laboral' => ['💼', 'Derecho Laboral'],
                                    'Derecho Familiar' => ['', 'Derecho Familiar'],
                                    'Familia' => ['', 'Derecho Familiar'],
                                    'Derecho Civil' => ['', 'Derecho Civil'],
                                    'Civil' => ['', 'Derecho Civil'],
                                    'Derecho Comercial' => ['', 'Derecho Comercial'],
                                    'Comercial' => ['', 'Derecho Comercial'],
                                    'Derecho Tributario' => ['', 'Derecho Tributario'],
                                    'Tributario' => ['', 'Derecho Tributario'],
                                    'Protección al Consumidor' => ['', 'Protección al Consumidor'],
                                    'Derechos Humanos' => ['', 'Derechos Humanos'],
                                    'Inmobiliario' => ['', 'Inmobiliario'],
                                    'Otros Casos' => ['⚖️', 'Otros Casos'],
                                    'Deudores' => ['💳', 'Abogado de Deudas'],
                                    'Extranjeria' => ['', 'Extranjería'],
                                    'Transito' => ['', 'Tránsito'],
                                    'Empresas' => ['', 'Empresas'],
                                ];
                                [$specEmoji, $specLabel] = $specMap[$spec] ?? ['⚖️', $spec ?: 'Abogado'];
                            ?>
                            <div class="card-topline">
                                <span class="card-bubble"><?= trim($specEmoji . ' ' . $specLabel) !== '' ? htmlspecialchars(trim($specEmoji . ' ' . $specLabel)) : htmlspecialchars($specLabel) ?></span>
                                <?php if (!empty($abg['destacado_hasta']) && strtotime((string)$abg['destacado_hasta']) >= time()): ?>
                                    <span class="card-bubble">⭐ Destacado</span>
                                <?php endif; ?>
                            </div>
                            <?php $uniTop = $isTopUniversity($abg['universidad'] ?? ''); ?>
                            <div class="card-title"><?= $uniTop ? '⭐ ' : '' ?><?= htmlspecialchars($abg['nombre'] ?? ('Abogado/a de ' . ($abg['especialidad'] ?? 'especialidad'))) ?></div>
                            <div class="card-sub"><?= $uniTop ? '⭐ ' : '🎓 ' ?><?= !empty($abg['universidad']) ? htmlspecialchars($abg['universidad']) : htmlspecialchars($abg['especialidad']) ?></div>
                            <div class="card-sub card-specialty">Abogado en casos de <?= htmlspecialchars($abg['especialidad'] ?? 'esta materia') ?></div>
                        </div>
                        </div>
                        <div class="card-body">
                            <?php $subm = []; if (!empty($abg['submaterias'])) { $tmp = json_decode((string)$abg['submaterias'], true); if (is_array($tmp)) { $subm = array_values(array_filter(array_map('strval', $tmp))); } } ?>
                            <?php $submSec = []; if (!empty($abg['submaterias_secundarias'])) { $tmpSec = json_decode((string)($abg['submaterias_secundarias'] ?? ''), true); if (is_array($tmpSec)) { $submSec = array_values(array_filter(array_map('strval', $tmpSec))); } } ?>
                            <?php if (!empty($subm) || !empty($abg['materia_secundaria']) || !empty($submSec)): ?>
                                <div class="card-submatter-strip">
                                    <?php foreach (array_slice($subm, 0, 3) as $sm): ?>
                                        <span class="submatter-chip"><?= htmlspecialchars($sm) ?></span>
                                    <?php endforeach; ?>
                                    <?php if (!empty($abg['materia_secundaria'])): ?>
                                        <span class="submatter-chip is-secondary"><?= htmlspecialchars((string)$abg['materia_secundaria']) ?></span>
                                    <?php endif; ?>
                                    <?php foreach (array_slice($submSec, 0, 1) as $smSec): ?>
                                        <span class="submatter-chip is-soft"><?= htmlspecialchars($smSec) ?></span>
                                    <?php endforeach; ?>
                                </div>
                            <?php endif; ?>
                            <?php $plazaShort = $plazaResumen($abg['ciudades_plaza'] ?? ''); ?>
                            <?php if (!empty($abg['materia_secundaria']) || $plazaShort !== ''): ?>
                                <div class="card-extra-line">
                                    
                                    <?php if ($plazaShort !== ''): ?><span class="mini-pill comunas-pill app-badge badge-accent">Comunas Frecuentes: <?= htmlspecialchars($plazaShort) ?></span><?php endif; ?>
                                </div>
                            <?php endif; ?>
                            <div class="card-mode-line">
                                                                <?php if (!empty($abg['abogado_verificado'])): ?>
                                    <span class="card-mode-pill pjud-ok app-badge badge-success">✅ Verificado PJUD</span>
                                <?php else: ?>
                                    <span class="card-mode-pill pjud-pending app-badge badge-warn">🟡 PJUD pendiente</span>
                                <?php endif; ?>
                                <span class="card-mode-pill region region-strong app-badge badge-info">Región: <?= !empty($abg['cobertura_nacional']) ? 'Todo Chile' : '' . htmlspecialchars((string)($abg['regiones_servicio'] ?: 'Cobertura local')) ?></span>
                                <?php if (!empty($abg['entrevista_presencial'])): ?><span class="card-mode-pill region app-badge badge-info">Entrevista presencial</span><?php endif; ?>
                            </div>
                            <?php $feedSummary = $buildFeedCardSummary($abg, $specLabel, []); ?>
                            <div class="card-meta-min card-summary-feed">
                                <span><?= htmlspecialchars($feedSummary) ?></span>
                                <?php if (!empty($abg['cobertura_nacional'])): ?><span>Presta servicios en todo Chile</span><?php endif; ?>
                            </div>
                            <div class="card-stats card-social-proof">
                                <span>👁️ <?= (int)($abg['vistas'] ?? 0) ?> vistas</span>
                                <span>👍 <?= (int)($abg['likes'] ?? 0) ?> likes</span>
                            </div>
                        </div>
                    </a>
                    <?php endforeach; ?>
                </div>
            </section>

            <?php if (!empty($abogadosRestantes)): ?>
                <div class="result-head">
                    <strong><?= (int)($total_profiles ?? count($abogados)) ?> abogados publicados</strong>
                    <span class="hint">Explora perfiles y compara especialidades</span>
                </div>
                <div class="grid desktop-grid<?= $isClientFeed ? " client-grid" : "" ?>">
                    <?php foreach ($abogadosRestantes as $abg): ?>
                    <?php $brandKey = trim((string)($abg['color_marca'] ?? 'azul')); ?>
                    <?php $brandRing = $brandRingMap[$brandKey] ?? $brandRingMap['azul']; ?>
                    <?php $brandTint = $brandTintMap[$brandKey] ?? $brandTintMap['azul']; ?>
                    <a href="/<?= $abg['slug'] ?>" class="card card-hover feed-card-item" style="display:block; --brand-ring: <?= htmlspecialchars($brandRing) ?>; --brand-tint: <?= htmlspecialchars($brandTint) ?>;">
                            <?php $fotoSrc = trim((string)($abg['foto_publica'] ?? '')); ?>
                            <?php
                                $abogadoNombreAlt = trim((string)($abg['nombre'] ?? ''));
                                $abogadoEspecialidadAlt = trim((string)($abg['especialidad'] ?? ''));
                                $fotoAlt = $abogadoNombreAlt !== ''
                                    ? ('Foto de perfil de ' . $abogadoNombreAlt . ($abogadoEspecialidadAlt !== '' ? ', abogado/a en ' . $abogadoEspecialidadAlt : ''))
                                    : 'Foto de perfil de abogado';
                            ?>
                            <div class="feed-card-head">
                            <?php if ($fotoSrc !== ''): ?>
                                <div class="media-frame" style="--brand-ring: <?= htmlspecialchars($brandRing) ?>">
                                    <img src="<?= htmlspecialchars($fotoSrc) ?>" alt="<?= htmlspecialchars($fotoAlt) ?>" style="width:100%; aspect-ratio:1/1; object-fit:cover;" loading="lazy" onerror="this.closest('.media-frame').classList.add('no-photo','demo-photo'); this.remove();">
                                </div>
                            <?php else: ?>
                                <div class="media-frame no-photo demo-photo" style="--brand-ring: <?= htmlspecialchars($brandRing) ?>">
                                    <div class="avatar-fallback"><?= htmlspecialchars(mb_strtoupper(mb_substr((string)($abg['nombre'] ?? 'A'), 0, 1))) ?></div>
                                </div>
                            <?php endif; ?>
                        <div class="feed-card-head-content">
                            <?php
                                $spec = (string)($abg['especialidad'] ?? '');
                                $specMap = [
                                    'Derecho Penal' => ['⚖️', 'Derecho Penal'],
                                    'Penal' => ['⚖️', 'Derecho Penal'],
                                    'Derecho Laboral' => ['💼', 'Derecho Laboral'],
                                    'Laboral' => ['💼', 'Derecho Laboral'],
                                    'Derecho Familiar' => ['', 'Derecho Familiar'],
                                    'Familia' => ['', 'Derecho Familiar'],
                                    'Derecho Civil' => ['', 'Derecho Civil'],
                                    'Civil' => ['', 'Derecho Civil'],
                                    'Derecho Comercial' => ['', 'Derecho Comercial'],
                                    'Comercial' => ['', 'Derecho Comercial'],
                                    'Derecho Tributario' => ['', 'Derecho Tributario'],
                                    'Tributario' => ['', 'Derecho Tributario'],
                                    'Protección al Consumidor' => ['', 'Protección al Consumidor'],
                                    'Derechos Humanos' => ['', 'Derechos Humanos'],
                                    'Inmobiliario' => ['', 'Inmobiliario'],
                                    'Otros Casos' => ['⚖️', 'Otros Casos'],
                                    'Deudores' => ['💳', 'Abogado de Deudas'],
                                    'Extranjeria' => ['', 'Extranjería'],
                                    'Transito' => ['', 'Tránsito'],
                                    'Empresas' => ['', 'Empresas'],
                                ];
                                [$specEmoji, $specLabel] = $specMap[$spec] ?? ['⚖️', $spec ?: 'Abogado'];
                            ?>
                            <div class="card-topline">
                                <span class="card-bubble"><?= trim($specEmoji . ' ' . $specLabel) !== '' ? htmlspecialchars(trim($specEmoji . ' ' . $specLabel)) : htmlspecialchars($specLabel) ?></span>
                                <?php if (!empty($abg['destacado_hasta']) && strtotime((string)$abg['destacado_hasta']) >= time()): ?>
                                    <span class="card-bubble">⭐ Destacado</span>
                                <?php endif; ?>
                            </div>
                            <?php $uniTop = $isTopUniversity($abg['universidad'] ?? ''); ?>
                            <div class="card-title"><?= $uniTop ? '⭐ ' : '' ?><?= htmlspecialchars($abg['nombre'] ?? ('Abogado/a de ' . ($abg['especialidad'] ?? 'especialidad'))) ?></div>
                            <div class="card-sub"><?= $uniTop ? '⭐ ' : '🎓 ' ?><?= !empty($abg['universidad']) ? htmlspecialchars($abg['universidad']) : htmlspecialchars($abg['especialidad']) ?></div>
                            <div class="card-sub card-specialty">Abogado en casos de <?= htmlspecialchars($abg['especialidad'] ?? 'esta materia') ?></div>
                        </div>
                        </div>
                        <div class="card-body">
                            <?php $subm = []; if (!empty($abg['submaterias'])) { $tmp = json_decode((string)$abg['submaterias'], true); if (is_array($tmp)) { $subm = array_values(array_filter(array_map('strval', $tmp))); } } ?>
                            <?php $submSec = []; if (!empty($abg['submaterias_secundarias'])) { $tmpSec = json_decode((string)($abg['submaterias_secundarias'] ?? ''), true); if (is_array($tmpSec)) { $submSec = array_values(array_filter(array_map('strval', $tmpSec))); } } ?>
                            <?php if (!empty($subm) || !empty($abg['materia_secundaria']) || !empty($submSec)): ?>
                                <div class="card-submatter-strip">
                                    <?php foreach (array_slice($subm, 0, 3) as $sm): ?>
                                        <span class="submatter-chip"><?= htmlspecialchars($sm) ?></span>
                                    <?php endforeach; ?>
                                    <?php if (!empty($abg['materia_secundaria'])): ?>
                                        <span class="submatter-chip is-secondary"><?= htmlspecialchars((string)$abg['materia_secundaria']) ?></span>
                                    <?php endif; ?>
                                    <?php foreach (array_slice($submSec, 0, 1) as $smSec): ?>
                                        <span class="submatter-chip is-soft"><?= htmlspecialchars($smSec) ?></span>
                                    <?php endforeach; ?>
                                </div>
                            <?php endif; ?>
                            <?php $plazaShort = $plazaResumen($abg['ciudades_plaza'] ?? ''); ?>
                            <?php if (!empty($abg['materia_secundaria']) || $plazaShort !== ''): ?>
                                <div class="card-extra-line">
                                    
                                    <?php if ($plazaShort !== ''): ?><span class="mini-pill comunas-pill app-badge badge-accent">Comunas Frecuentes: <?= htmlspecialchars($plazaShort) ?></span><?php endif; ?>
                                </div>
                            <?php endif; ?>
                            <div class="card-mode-line">
                                                                <?php if (!empty($abg['abogado_verificado'])): ?>
                                    <span class="card-mode-pill pjud-ok app-badge badge-success">✅ Verificado PJUD</span>
                                <?php else: ?>
                                    <span class="card-mode-pill pjud-pending app-badge badge-warn">🟡 PJUD pendiente</span>
                                <?php endif; ?>
                                <span class="card-mode-pill region region-strong app-badge badge-info">Región: <?= !empty($abg['cobertura_nacional']) ? 'Todo Chile' : '' . htmlspecialchars((string)($abg['regiones_servicio'] ?: 'Cobertura local')) ?></span>
                                <?php if (!empty($abg['entrevista_presencial'])): ?><span class="card-mode-pill region app-badge badge-info">Entrevista presencial</span><?php endif; ?>
                            </div>
                            <?php $feedSummary = $buildFeedCardSummary($abg, $specLabel, []); ?>
                            <div class="card-meta-min card-summary-feed">
                                <span><?= htmlspecialchars($feedSummary) ?></span>
                                <?php if (!empty($abg['cobertura_nacional'])): ?><span>Presta servicios en todo Chile</span><?php endif; ?>
                            </div>
                            <div class="card-stats card-social-proof">
                                <span>👁️ <?= (int)($abg['vistas'] ?? 0) ?> vistas</span>
                                <span>👍 <?= (int)($abg['likes'] ?? 0) ?> likes</span>
                            </div>
                        </div>
                    </a>
                    <?php endforeach; ?>
                </div>
            <?php endif; ?>
            <?php if (($total_pages ?? 1) > 1): ?>
                <?php
                    $currentPage = max(1, (int)($page ?? 1));
                    $lastPage = max(1, (int)($total_pages ?? 1));
                    $baseParams = $_GET;
                    $prevHref = null;
                    $nextHref = null;
                    if ($currentPage > 1) {
                        $tmp = $baseParams;
                        $tmp['page'] = $currentPage - 1;
                        $prevHref = '/explorar?' . http_build_query($tmp);
                    }
                    if ($currentPage < $lastPage) {
                        $tmp = $baseParams;
                        $tmp['page'] = $currentPage + 1;
                        $nextHref = '/explorar?' . http_build_query($tmp);
                    }
                ?>
                <nav class="pager" aria-label="Paginación de perfiles">
                    <?php if ($prevHref): ?><a class="btn btn-ghost" href="<?= htmlspecialchars($prevHref) ?>">← Anterior</a><?php endif; ?>
                    <span class="page-label">Página <?= $currentPage ?> de <?= $lastPage ?> · hasta 15 perfiles</span>
                    <?php if ($nextHref): ?><a class="btn btn-primary" href="<?= htmlspecialchars($nextHref) ?>">Siguiente →</a><?php endif; ?>
                </nav>
            <?php endif; ?>
        <?php endif; ?>

        <section style="margin-top: 18px; display:none;">
            <div class="cta-bar"></div>
        </section>

        <div class="explorar-footnote">
            <a href="/nosotros">Sobre el proyecto</a>
        </div>

        <div id="feedLoadSentinel" class="feed-load-sentinel" style="display:none"></div>
    </main>

    <div class="bottom-nav app-primary-nav<?= empty($_SESSION['user_id']) ? ' feed-guest-nav no-google-cta' : '' ?>">
        <a href="/explorar" class="active-nav">Explorar</a>
        <?php if(isset($_SESSION['user_id'])): ?>
            <?php if ($isLawyerFeed): ?>
                <a href="<?= htmlspecialchars($currentFeedSlug !== '' ? ('/' . $currentFeedSlug) : '/mi-tarjeta') ?>">Ver mi perfil</a>
            <?php else: ?>
                <a href="/panel">Mi panel</a>
            <?php endif; ?>
            <a href="/logout" class="app-primary-cta-link">Salir</a>
        <?php else: ?>
            <a href="/login-google" class="app-primary-cta-link">Regístrate</a>
        <?php endif; ?>
    </div>

    <?php if (empty($_SESSION['user_id'])): ?>
    <a href="/login-google" class="google-login-banner" aria-label="Regístrate con Google: clientes envían formulario y abogados reciben mensajes">
        <span class="google-mini-badge" aria-hidden="true"><svg viewBox="0 0 24 24"><path fill="#EA4335" d="M12 10.2v3.9h5.5c-.2 1.3-1.5 3.9-5.5 3.9-3.3 0-6-2.7-6-6s2.7-6 6-6c1.9 0 3.2.8 3.9 1.5l2.7-2.6C16.9 3.3 14.7 2.4 12 2.4 6.7 2.4 2.4 6.7 2.4 12S6.7 21.6 12 21.6c6.9 0 9.1-4.8 9.1-7.3 0-.5 0-.9-.1-1.2H12z"/></svg></span>
        <span class="banner-text">Regístrate con Google: clientes envían formulario, abogados reciben y tramitan potenciales clientes</span><span class="banner-text-mobile">Regístrate y activa mensajes</span>
    </a>
    <?php endif; ?>
    <script src="/feed-page.js?v=2026022704"></script>
</body>
</html>
